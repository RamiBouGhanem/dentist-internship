# Copy to .env for local custom credentials. Never commit .env.
ADMIN_EMAIL=coach@demo.com
ADMIN_PASSWORD=CoachDemo2026!
# Production must use unique credentials (at least 12 password characters).
# DATA_DIR=/var/data
# Optional: exact public origin, without a trailing slash, when needed behind a proxy.
# APP_ORIGIN=https://your-domain.example
